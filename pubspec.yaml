name: smart_house

version: 1.0.0+1

environment:
  sdk: ">=2.2.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  after_layout:
  response:
  provider:
  font_awesome_flutter:
  flutter_sequence_animation: ^3.0.1
  bezier_chart: ^1.0.16

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
  assets:
  - assets/